import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
    setsReps: {
        // width: '70px',
        width: '48%',
        // m: '0 5px',
    },
}));

export default useStyles;