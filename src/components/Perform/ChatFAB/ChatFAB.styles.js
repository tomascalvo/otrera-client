import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  fab: {
    position: "fixed",
    bottom: 24,
    right: 24,
    zIndex: 2,
  },
}));

export default useStyles;
